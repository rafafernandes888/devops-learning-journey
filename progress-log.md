# Progress Log

## Day 1

- Created the DevOps learning repository structure.
- Set up folders for Linux, Networking, Git, Docker, CI/CD, AWS and Monitoring.
- Started organizing a practical roadmap to study DevOps fundamentals.
- Reviewed the first Linux terminal commands and basic project organization.
- Practiced reading and filtering log files using `cat`, `grep`, `grep -n` and `grep -E`.
- Practiced reading parts of log files using `head`, `tail` and `tail -f`.
- Reviewed Linux file permissions using `r`, `w`, `x` and `chmod`.
- Created and executed a basic shell script using `#!/bin/bash`.
- Practiced Linux process management using `ps`, `ps aux`, `grep`, `sleep`, `kill` and `top`.

## Day 2

- Continued the Linux fundamentals module.
- Studied Linux services using `systemctl`.
- Practiced checking service status, active/enabled states and failed services.
- Reviewed `cron` as a scheduled task service and `rsyslog` as a system logging service.
- Practiced basic Linux networking commands such as `ip addr`, `ping`, `curl`, `ss` and `resolvectl`.
- Reviewed system logs using `journalctl` and `/var/log/syslog`.